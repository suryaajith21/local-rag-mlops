name: RAG Evaluation Pipeline v2

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  evaluate-rag:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install / sync dependencies
        run: |
          conda run -n rag-ops python -m pip install -r requirements.txt -q

      - name: Run ingestion pipeline (all PDFs in data/)
        run: |
          conda run -n rag-ops python -c "
          import asyncio
          from src.ingestion.pipeline import run_ingestion_pipeline
          summary = asyncio.run(run_ingestion_pipeline(clear_existing=True))
          print('Ingestion summary:')
          for k, v in summary.items():
              print(f'  {k}: {v}')
          files = summary.get('files_processed', 0)
          assert files > 0, f'No files ingested - check data/ directory'
          "

      - name: Start FastAPI server
        run: |
          start /B conda run -n rag-ops uvicorn src.api.server:app --host 0.0.0.0 --port 8000
          timeout /T 20 /NOBREAK

      - name: Verify server health
        run: |
          conda run -n rag-ops python -c "
          import httpx, json
          r = httpx.get('http://localhost:8000/health')
          r.raise_for_status()
          data = r.json()
          print('Health:', json.dumps(data, indent=2))
          assert data['status'] == 'ok', 'Server unhealthy'
          assert data['vector_chunks'] > 0, 'Vector store is empty after ingestion'
          "

      - name: Run evaluation (eval_v2.py)
        run: |
          conda run -n rag-ops python evaluation/eval_v2.py --server http://localhost:8000

      - name: Stop FastAPI server
        if: always()
        shell: powershell
        run: |
          $result = netstat -ano | Select-String ":8000\s.*LISTENING"
          if ($result) {
            $server_pid = ($result.ToString().Trim() -split '\s+')[-1]
            taskkill /F /PID $server_pid
            Write-Host "Killed PID $server_pid"
          } else {
            Write-Host "No process on port 8000, nothing to kill"
          }

      - name: Upload evaluation artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: eval-results-v2-${{ github.run_number }}
          path: |
            evaluation/v2/evaluation_results_v2.csv
            evaluation/v2/summary.json
            evaluation/v2/test_dataset.json
          if-no-files-found: warn
